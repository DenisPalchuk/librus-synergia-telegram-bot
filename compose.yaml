services:
  bot:
    build: .
    image: librus-telegram-bot:local
    restart: unless-stopped
    stop_grace_period: 4m
    env_file: .env
    environment:
      TZ: Europe/Warsaw
    volumes:
      - librus_state:/data
    read_only: true

volumes:
  librus_state:
